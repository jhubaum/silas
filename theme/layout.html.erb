<!doctype HTML>

<html>
  <head>
    <meta charset="utf-8">
    <%= css "style.css" %>
    <%= css "code.css" %>
    <title>Johannes Huwald ~ <%= title %></title>
  </head>
  <body>
    <nav class="nav">
      <div class="nav-container">
        <a href="<%= website.url %>" id="nav-title">
          <h2>Johannes Huwald</h2>
        </a>
        <ul>
          <% header.each do |link| %>
          <li><%= link.to_html nil %></li>
          <% end %>
        </ul>
      </div>
      <hr size="0.5px" width="90%">
    </nav>
    <%= yield %>
  </body>
</html>
